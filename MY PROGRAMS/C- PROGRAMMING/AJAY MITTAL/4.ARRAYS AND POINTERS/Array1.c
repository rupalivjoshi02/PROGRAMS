/* a program to illustrate the use of subscript operator. */


#include<stdio.h>

int main()
{
	int arr[3]={10,20,30};

	printf("Elements of an array are: ");
	printf("%d %d %d\n",arr[0],arr[1],arr[2]);

	return(0);
}
